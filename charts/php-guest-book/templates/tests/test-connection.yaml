apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "php-guest-book.fullname" . }}-test-connection"
  labels:
    {{- include "php-guest-book.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "php-guest-book.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
